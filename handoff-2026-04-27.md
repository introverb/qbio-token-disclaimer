# QBIO Token Disclaimer — Session Handoff

> ## ⚠️ READ FIRST — UNVERIFIED DEPLOY STATE
>
> **The previous session set up GitHub Pages, but Olli described the project as "deployed to Cloudflare Pages."** These two facts have not been reconciled. Cloudflare Pages was *not* configured during the previous session — it may have been done out-of-band, may be in progress, or may be aspirational.
>
> **Your first priority on this session is to figure out the real deploy state before any other work.** See §9 for the exact ask. Do not assume either GH Pages or CF Pages is the canonical deploy until confirmed.

---

**Written:** 2026-04-27
**Repo:** https://github.com/introverb/qbio-token-disclaimer
**Live URL (GitHub Pages — verified):** https://introverb.github.io/qbio-token-disclaimer/
**Live URL (Cloudflare Pages — claimed but unverified):** unknown
**New project path (after move):** `C:\Users\ollip\OneDrive\Desktop\coding-projects\qbio-token-disclaimer\`

---

## 1. What this is

The **QBIO Token Disclaimer** is a single-page legal disclaimer site for the QBIO governance token issued by Quantum Biology DAO (QBIO DAO). It's a static HTML page (no build step, no JS framework) brand-matched to the main DAO site at https://www.quantumbiology.xyz. Stack: hand-rolled HTML + CSS, self-hosted Apercu (sans) and Chap (display serif) brand fonts via `@font-face`. Currently served via **GitHub Pages**; user has indicated a planned migration to **Cloudflare Pages** — verify deployment state on first session.

---

## 2. Current state

**Done:**
- `index.html` — full disclaimer text (3 sections: Legal Disclaimer / Holder Representations / Restrictions on Participation), brand-styled
- Logo SVG inlined into `index.html`, recolored cream + pink for the dark nav banner
- Brand fonts wired via `@font-face` (6 Apercu weights + Chap Regular)
- Header banner image (`Assets/header-banner.png`) sits as a thin 110px nav bar at top
- Nav: cream logo (links to quantumbiology.xyz) + "← Back to quantumbiology.xyz" / "← Back to Discord" cream links, pink hover
- Footer with quantumbiology.xyz / Discord / X-Twitter links + copyright
- All `$QBIO` token mentions stripped to `QBIO` per team request
- GitHub Pages enabled on `main` branch, root `/`, HTTPS enforced
- Repo public on github.com/introverb/qbio-token-disclaimer

**Locked-in design decisions:**
- Cream background, dark-purple text/accents, pink reserved for logo + hover states
- Apercu for body/UI, Chap for h1 only (only one Chap weight available — see §6)
- "$" prefix on `QBIO` tokens removed everywhere
- Banner doubles as nav bar (dark purple image, cream content overlaid)
- Numbered list uses 01/02 zero-padded markers in light purple
- All-caps legal blocks rendered as left-bordered callouts on faint purple wash

**Color palette (used in CSS variables in `index.html`):**

| Hex | Name | Used for |
|---|---|---|
| `#EEE8DF` | Cream | Page bg, logo cream fills, nav text |
| `#E5DED2` | Cream dim | Footer bg |
| `#2D1B30` | Dark purple | Body strong, nav fallback, borders (alpha) |
| `#7D4A6E` | Light purple | h2 labels, list markers, hover accent |
| `#D57DB2` | Brand pink | Logo accents, nav hover |
| `#1A1416` | Near-black | Body paragraph text |
| `#3A3036` | Soft text | Caps blocks, footer copyright |

`--purple-mid: #4A2E4E` is defined but currently unused — safe to ignore or wire into a future hover state.

---

## 3. Queue / open items

- [ ] **Confirm Cloudflare Pages deployment.** User mentioned CF Pages is the deploy target; this session only configured GH Pages. Verify with user, capture domain + DNS + build config.
- [ ] **Transfer GitHub repo to DAO's official org** once ops creates the DAO GitHub account (currently on Olli's personal `introverb`). After transfer, run `git remote set-url origin <new-url>` locally and update any external links to the new owner's `github.io` URL.
- [ ] **Apercu/Chap font licensing review** with DAO ops — see §8 Gotchas. Currently committed to a public repo; standard web-font practice but technically a Colophon Foundry license grey area.
- [ ] **(Optional) Convert Apercu `.otf` → `.woff2`** for ~50% smaller file sizes / faster page load.
- [ ] **(Optional) Source Chap Medium** — site actually uses Chap Medium; we only have Chap Regular. Slight typographic mismatch in h1.
- [ ] **(Optional) Favicon.** Currently none. Use the hexagon logo mark.
- [ ] **(Optional) Custom domain** for the disclaimer (e.g. `disclaimer.quantumbiology.xyz`) once on Cloudflare.

---

## 4. Last commits — verbatim from `git log --oneline -10`

```
edc01a3 Exclude QBIO NEWS subfolder (has its own repo)
87e0a33 Switch to brand fonts Apercu + Chap
97c00c5 Rename disclaimer.html to index.html for Pages root URL
fb35ed1 Add QBIO token disclaimer page
```

(The repo only has 4 commits — full history shown.)

**Gloss:**

- **`edc01a3` Exclude QBIO NEWS subfolder** — added `QBIO NEWS/` to `.gitignore`. QBIO NEWS used to live as a subfolder of `QBIO DAO\` but was promoted to its own git repo. This commit prevents accidentally pulling its files into the disclaimer repo.
- **`87e0a33` Switch to brand fonts Apercu + Chap** — replaced placeholder Google Fonts (Cormorant Garamond / Syne / DM Mono) with self-hosted Colophon Foundry brand fonts under `Assets/fonts/`. Updated all `font-family` declarations across the CSS.
- **`97c00c5` Rename disclaimer.html to index.html** — renamed via `git mv` so the Pages URL is `/qbio-token-disclaimer/` instead of `/qbio-token-disclaimer/disclaimer.html`. Any external links to `disclaimer.html` will 404 — they need to be updated to the root path.
- **`fb35ed1` Add QBIO token disclaimer page** — initial commit. Brought in `disclaimer.html`, `Assets/header-banner.png`, and `.gitignore`.

---

## 5. Last meaningful actions in the previous session

1. **Switched typography to brand fonts** (commit `87e0a33`). Edited 7 CSS rule blocks in `index.html`: removed Google Fonts `<link>`, added 7 `@font-face` declarations pointing at `Assets/fonts/`, rewrote body / h1 / h2 / nav-links / caps-block / list marker / footer font stacks.
2. **Excluded QBIO NEWS from the repo** (commit `edc01a3`). User updated `.gitignore` to add `QBIO NEWS/` so the disclaimer repo doesn't track that adjacent project.
3. **Renamed `disclaimer.html` → `index.html`** (commit `97c00c5`) and pushed. Enabled GitHub Pages via `gh api repos/introverb/qbio-token-disclaimer/pages -X POST`. Verified live with `curl -sI` returning HTTP 200.
4. **Committed font assets** to `Assets/fonts/` (~600 KB total): all 6 Apercu `.otf` files + `Chap-Regular.woff2`.
5. **Color palette documented** for the user (cream / 3 purples / pink / 2 text shades).

---

## 6. Brand / asset notes

### Fonts — `Assets/fonts/`
| File | Maps to |
|---|---|
| `apercu_regular_pro.otf` | Apercu 400 normal |
| `apercu_regular_italic_pro.otf` | Apercu 400 italic |
| `apercu_medium_pro.otf` | Apercu 500 normal |
| `apercu_medium_italic_pro.otf` | Apercu 500 italic |
| `apercu_bold_pro.otf` | Apercu 700 normal |
| `apercu_bold_italic_pro.otf` | Apercu 700 italic |
| `Chap-Regular.woff2` | Chap 400 normal |

**Caveat:** The live quantumbiology.xyz site actually uses **Chap Medium**, not Chap Regular. We have Regular only — slight visual mismatch in h1. Source Chap Medium when convenient.

### Images — `Assets/`
- `header-banner.png` — **in use** as the top nav-bar background. Wide purple strip with chain-of-cubes pattern.
- `blobs-hero.png`, `cube-hero.png`, `dna-hero.png`, `strand-hero.png`, `white-strand-hero.png` — **NOT in use**, NOT committed (untracked). Alternate hero options retained for future use.

### Logo
- `Assets/quantum-bio-dao-logo-light.svg` — original brand logo (cream `#EEE8DF` letterforms + pink `#D57DB2` accents on dark bg). **NOT referenced from `index.html`** — the logo is inlined directly into the HTML so we can recolor it inline. Keep this file as a reference asset.

### `Assets/token-disclosure.html` — IMPORTANT
This is an **earlier draft** of the disclaimer with a dark-themed layout (bg `#25101D`, cream text) and a different (incorrect) font stack (Cormorant Garamond / Syne / DM Mono). It is **not** the live page and **not** what's deployed. The current live page is `index.html` at the repo root. Keep `token-disclosure.html` for reference if helpful, but don't deploy it or confuse it with the live file.

---

## 7. Deploy notes

### What's actually wired up (verified this session)
- **GitHub Pages** on `introverb/qbio-token-disclaimer`, source `main` branch, path `/`, HTTPS enforced
- Live at https://introverb.github.io/qbio-token-disclaimer/
- Build status `built` as of `2026-04-17 19:57:50Z`

### Cloudflare Pages (per user note — NOT verified this session)
User has indicated Cloudflare Pages is the deployment target. Was not configured during this session. **First task next session: confirm CF Pages status with user.** If migrating:
- Build command: *(none — static site)*
- Output dir: `/` (root)
- Branch: `main`
- Custom domain: TBD (likely `disclaimer.quantumbiology.xyz` or similar — confirm with user)
- DNS: needs CNAME pointing to the CF Pages URL once provisioned

### After repo transfer to DAO org
The GitHub Pages URL will rebase to `https://<dao-org>.github.io/qbio-token-disclaimer/`. The repo URL also changes — update local remote with `git remote set-url origin git@github.com:<dao-org>/qbio-token-disclaimer.git`.

---

## 8. Gotchas

- **`SWG/` folder is intentionally left behind** at the original `C:\Users\ollip\OneDrive\Desktop\QBIO DAO\` location. It contains DAO governance PDFs and is **not** part of this repo. Do not add it. Only `index.html`, `Assets/`, `.git/`, `.gitignore`, and this handoff doc move to `coding-projects/qbio-token-disclaimer/`.
- **`QBIO NEWS/` is a separate repo.** It was once a subfolder here, then promoted out. The `.gitignore` (lines 6–7) excludes it — leave that exclusion in place even though the folder is no longer adjacent after the move.
- **Apercu + Chap are paid Colophon Foundry fonts.** The DAO has a license (live site uses them). They're committed to a public GitHub repo here, which is standard web-font practice but a license grey area. If pushed back, options: (a) move fonts to a non-indexed location, (b) link directly to DAO's Webflow CDN URLs (`cdn.prod.website-files.com/66967259442bcd2ad28bf3b1/...`), or (c) keep as-is.
- **GitHub account:** repo is on Olli's personal `introverb`. The DAO's official GH org doesn't exist yet (ops is creating it). Once it does, transfer the repo via Settings → Danger Zone → Transfer ownership, then update local remote.
- **Old `disclaimer.html` path is dead.** Any DAO docs / Discord pins / external docs referencing `…/qbio-token-disclaimer/disclaimer.html` will 404 — the file is now `index.html` at root. The clean URL is just `https://introverb.github.io/qbio-token-disclaimer/`.
- **OneDrive sync + git can occasionally race** — if you see weird "file in use" errors during `git add`, pause OneDrive sync briefly.
- **The desktop Claude Code app doesn't currently support a folder picker** (known limitation, GitHub issue #36175). After the move, starting a fresh session in `coding-projects/qbio-token-disclaimer/` requires either: (a) the CLI (`claude` from terminal in that folder), or (b) accepting that sessions show under whatever project the desktop app is anchored to.

---

## 9. FIRST PRIORITY for next session — resolve the deploy ambiguity

**Before any other work, ask Olli the following and update §7 + the top-of-file notice with the answers:**

1. "Is Cloudflare Pages already set up and serving the disclaimer? If yes — what's the live URL and is a custom domain configured?"
2. "If not yet set up: do you want to migrate off GitHub Pages to Cloudflare now, run them in parallel, or stay on GitHub Pages until after the repo transfers to the DAO org?"
3. "If CF is the target, what domain should it serve from?" (Likely candidates: `disclaimer.quantumbiology.xyz`, a path on the main `quantumbiology.xyz` site, or a standalone domain.)

Once you have answers, **(a) edit the top-of-file notice to remove the warning and state the canonical deploy URL, (b) update §7 with real config, and (c) commit the updated handoff** before starting any other task. The rest of the open queue (§3) is blocked on knowing where the page actually lives.
